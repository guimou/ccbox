# Namespace-scoped resources for one coding pod. Deploy through an overlay
# that sets the namespace and the RWX storage class (see overlays/example).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - serviceaccount.yaml
  - rolebinding.yaml
  - pvc.yaml
  - deployment.yaml
