# OS packages to install in the container
# One package per line, comments start with #

# Audio
alsa-plugins-pulseaudio
pulseaudio-utils
ffmpeg-free

# Core utilities
bash-completion
bind-utils
curl
findutils
hostname
jq
nano
procps-ng
vim-enhanced

# Locales (en_* UTF-8 locales, avoids setlocale warnings when the host LANG is forwarded)
glibc-langpack-en

# Version control
git
gh

# Node.js (Fedora 44+ uses versioned nodejs packages)
nodejs24
nodejs24-bin
nodejs24-npm-bin
pnpm

# Networking/firewall
iproute
ipset
iptables

# Python development
python3
python3-pip
python3-devel
python3-virtualenv

# Build tools
make
gcc
gcc-c++
cmake
pkg-config

# Native extension dependencies
openssl-devel
libffi-devel
zlib-devel
sqlite-devel

# Clipboard tools (for image pasting support)
xclip
wl-clipboard

# Search tools (essential for code navigation)
ripgrep
fd-find

# File utilities
tree
less
unzip
xz
which
file
poppler-utils

# Development utilities
diffutils
patch
ShellCheck

# Network utilities
openssh-clients

# Database clients
sqlite
postgresql
mysql
redis

# Diagram generation (commonly used for documentation)
graphviz

# Code quality tools (used by plugins)
ruff

# Process/port inspection
lsof

# YAML processing (complement to jq for Helm/kubectl workflows)
yq

# Container/orchestration tools
helm
kubernetes1.35-client

# Configuration management (DevOps skills)
ansible

# Sandbox support (Claude Code subprocess isolation)
bubblewrap
socat

# Terminal multiplexer (for agent teams split-pane mode)
tmux

# Browser (for Playwright MCP, browser automation)
chromium
